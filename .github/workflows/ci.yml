name: CI
on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Run SquatCounter tests
        run: ./build.sh --test
      - name: Release build (universal .app)
        run: ./build.sh
